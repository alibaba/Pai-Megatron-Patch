log_dir: ${log_dir:"None"}
exp_name: ${exp_name:test}
tensorboard_dir: ${tensorboard_dir:"None"}
enable_tensorboard: ${enable_tensorboard:False}

enable_wandb: ${enable_wandb:False}
wandb_dir: ${wandb_dir}
wandb_project: ${WANDB_PROJECT}
wandb_id: ${WANDB_RUN_ID}
wandb_name: ${WANDB_NAME}
wandb_resume: ${WANDB_RESUME:allow}